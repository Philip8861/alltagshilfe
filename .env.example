# Öffentliche Basis-URL der Website (für Canonical, OG, Sitemap).
# Beim Umzug auf anderen Server nur hier anpassen – keine Änderung im Code nötig.
NEXT_PUBLIC_SITE_URL=https://example.com

# Optional: Kanonische URL nur für Supabase Auth-Redirects (Passwort-Reset). Serverseitig, nicht NEXT_PUBLIC.
# Setzen, wenn NEXT_PUBLIC_SITE_URL in Production noch localhost ist oder die E-Mail-Links eine feste Domain brauchen.
# AUTH_REDIRECT_BASE_URL=https://www.ihre-domain.de
#
# --- Supabase Auth: E-Mail von „Alltagshilfe-Süd“ + klickbare Links ---
# 1) Supabase Dashboard → Authentication → URL configuration:
#    - Site URL = exakt eure Live-URL (https://…), NICHT localhost.
#    - Additional Redirect URLs: jeweils mit Wildcard, z. B.:
#        https://www.ihre-domain.de/auth/callback**
#        https://ihre-domain.de/auth/callback**
#        Passwort-Reset (ohne ?next=, eigener Callback): z. B.
#        https://www.ihre-domain.de/auth/callback/passwort**
#        https://ihre-domain.de/auth/callback/passwort**
#      (ohne Eintrag leitet Supabase oft auf Site URL um — dann wirkt der Link „nicht erreichbar“ oder landet falsch.)
# 2) E-Mail-Absender wie die Website: Project Settings → Auth → SMTP Settings → Custom SMTP aktivieren
#    (dieselben Zugangsdaten wie SMTP_HOST / SMTP_USER / … oben; Absendername z. B. „Alltagshilfe-Süd“).
# 3) E-Mail-Text/HTML: Authentication → Email Templates → „Reset password“ — Vorlage unter
#    supabase/email-templates/password-recovery-markenlayout.html einfügen.

# Optional: Indeed-Stellen-URL (Karriere-Seite)
# NEXT_PUBLIC_INDEED_JOBS_URL=https://de.indeed.com/cmp/Alltagshilfe-Süd/jobs

# --- Statistik (Google) ---
# GA4 erfolgt über den Google Tag Manager (Container GTM-NTZ7VJJC – fest im Root-Layout).
# Der Banner lädt gtm.js erst nach Einwilligung „Statistik“ und/oder „Marketing“; Consent Mode v2 liegt per beforeInteractive-Stubs bereit.
# Keine NEXT_PUBLIC_* Mess-ID mehr nötig – GA4 Measurement ID nur im GTM-UI konfigurieren (nicht doppelt im Frontend ausspielen).
#
# --- Marketing (Meta Pixel) ---
# Lädt erst nach Einwilligung „Marketing“ (components/analytics/MetaPixel.tsx).
# Standard-ID im Code: 1359733255202163 — optional überschreibbar:
# NEXT_PUBLIC_META_PIXEL_ID=1359733255202163
# Falls im GTM-Container ebenfalls ein Meta-Pixel-Tag aktiv ist: dort deaktivieren (sonst doppelte PageViews).
#
# --- Google Search Console (Verifizierung per Meta-Tag) ---
# Aus search.google.com/search-console → URL-Präfix-Property → „HTML-Tag“-Methode.
# Der Wert ist der `content`-Wert des angezeigten <meta name="google-site-verification" …>-Tags.
# Empfohlen: Domain-Property + DNS-TXT-Verifizierung beim Domain-Anbieter (DNS-Variante hier nicht nötig).
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Interne Benachrichtigungen (Kontakt, Karriere, Pflegebox) per SMTP — z. B. ALL-INKL:
# Ausgangsserver im KAS: typisch DEIN-KAS-LOGIN.kasserver.com (nicht die Domain als Host — siehe ALL-INKL Hilfe zu SSL/TLS).
# Port 465 + SMTP_SECURE=true (SSL) ODER Port 587 + SMTP_SECURE=false (STARTTLS)
# SMTP_USER = vollständige E-Mail-Adresse des Postfachs; SMTP_PASS = Passwort des Postfachs
# NOTIFICATION_TO = Fallback für alle Kanäle, wenn die spezifische Variable fehlt (kommagetrennt möglich)
# Pro Kanal eigene Ziele (optional; überschreibt den Fallback für diesen Kanal):
# NOTIFICATION_TO_CONTACT=kontakt@ihre-domain.de
# Kontaktformular: Thema „Karriere“ → zuerst NOTIFICATION_TO_CONTACT_TOPIC_KARRIERE, sonst dieselbe Adresse wie Bewerbungsformular (NOTIFICATION_TO_KARRIERE), sonst NOTIFICATION_TO_CONTACT:
# NOTIFICATION_TO_CONTACT_TOPIC_KARRIERE=daniel.niebauer@alltagshilfe-sued.de
# NOTIFICATION_TO_KARRIERE=karriere@ihre-domain.de
# NOTIFICATION_TO_PFLEGEBOX=pflegebox@ihre-domain.de
# Angebots-Popup „Betriebliche Pflegeberatung“ (optional; Standard im Code: philip.sonntag@alltagshilfe-sued.de)
# NOTIFICATION_TO_BETRIEBLICH_ANGEBOT=
# SMTP_HOST=w0123456.kasserver.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=noreply@ihre-domain.de
# SMTP_PASS=
# NOTIFICATION_TO=info@ihre-domain.de
# Optional: Absenderzeile (sonst: Alltagshilfe-Süd <SMTP_USER>)
# MAIL_FROM="Alltagshilfe <noreply@ihre-domain.de>"
# Nur serverseitig, niemals NEXT_PUBLIC_*
# Optional: Footer-Texte Partner-Registrierungs-Bestätigung (Admin legt Partner an) — sonst Impressum-/Standardwerte im Code
# PARTNER_REGISTRATION_MAIL_KONTAKT="…\n…"  (mehrzeilig)
# PARTNER_REGISTRATION_MAIL_TELEFON=08334 / 9893330
# PARTNER_REGISTRATION_MAIL_TEAM_EMAIL=info@alltagshilfe-sued.de
# PARTNER_REGISTRATION_MAIL_WEBSITE=https://www.alltagshilfe-sued.de
#
# Signatur für Standort-Kontaktformulare (HMAC). In Production setzen, sonst kein CC an lokale Standort-Adressen.
# Mindestens 32 Zeichen Zufallswert. Ohne diesen Schlüssel: /kontakt bleibt unverändert; Standortseiten CC nur mit Dev-Fallback.
# STANDORT_CONTACT_FORM_SECRET=

# Partnerportal (Supabase, EU-Region empfohlen — siehe supabase/migrations/001_partner_portal.sql)
# Kurzname-Login: ohne @ wird name@<DOMAIN> an Supabase geschickt (Standard partners.invalid).
# NEXT_PUBLIC_PARTNER_AUTH_EMAIL_DOMAIN=partners.invalid
# NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
# Service Role nur serverseitig (Pflegebox-Abschluss, Partner anlegen in Verwaltung, Passwort-Reset-E-Mail über Website-SMTP) — niemals NEXT_PUBLIC oder Client
# SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Partner-Verwaltung (/partner/admin-login → /partner/admin): Zugang NUR aus Env, niemals im Code committen.
# PARTNER_SYSTEM_ADMIN_USER=admin
# PARTNER_SYSTEM_ADMIN_PASSWORD=Ihr-starkes-Passwort
# PARTNER_SYSTEM_ADMIN_SECRET=mindestens-24-zeichen-zufaellig
#
# Optional: Absoluter oder projektrelativer Pfad zur Blanko-PDF für Formular-V1 (Admin-Vorschau / fillFormV1Pdf).
# Standard: private/forms/form-v1-blank.pdf
# FORM_V1_PDF_TEMPLATE_PATH=private/forms/form-v1-blank.pdf
#
# Monatsabrechnung Partner-Provisionen: GET /api/cron/partner-payout-settle
# In Vercel: CRON_SECRET setzen (Cron sendet Authorization: Bearer …). Optional zusätzlich PARTNER_PAYOUT_CRON_SECRET für manuellen Aufruf per ?secret=
# CRON_SECRET=
# PARTNER_PAYOUT_CRON_SECRET=
